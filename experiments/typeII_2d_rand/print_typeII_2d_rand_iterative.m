exp_typeII_2d_rand_settings;

originalPath = path;
addpath("../");

for it_alpha = 1 : num_alpha
    alpha = alpha_list(it_alpha);
    alpha_string = alpha_string_list(it_alpha);
    alpha_display = alpha_display_list(it_alpha);
    caption_name = "Time cost, " + ...
        "iteration number, " + ...
        "relative residual " + ...
        "and relative error of " + ...
        "lsqr and plsqr " + ...
        "on a random grid. " + ...
        "\(M = " + alpha_display + "N\).";
    label_name = "typeII_2d_rand_iterative_" + alpha_string;

    result_list = [];
    result_list_lsqr = [];
    for it_n = 1 : num_n
        p = p_list(it_n);

        load("./data/typeII_2d_results_lsqr" ...
            + "_" + string(p) ...
            + "_" + string(alpha) ...
            + ".mat");
        result_list_lsqr = [result_list_lsqr; result];

        result_list_tmp = [];
        for it_hss_tol = 1 : num_tol_hss
            tol_hss = tol_hss_list(it_hss_tol);
            load("./data/typeII_2d_results" ...
                + "_" + string(p) ...
                + "_" + string(alpha) ...
                + "_" + string(tol_hss) ...
                + ".mat");
            result_list_tmp = [result_list_tmp, result];
        end
        result_list = [result_list; result_list_tmp];
    end

    print_table_iterative(...
        result_list_lsqr, result_list, ...
        caption_name, label_name, ...
        tol_hss_display_list);
end

path(originalPath);
