exp_typeII_2d_rand_settings;

for it_alpha = 1 : num_alpha
    for it_p = 1 : num_n
        tol_hss = tol_hss_list(1);
        alpha = alpha_list(it_alpha);
        p = p_list(it_p);

        load("./data/typeII_2d_points" ...
            + "_" + string(p) ...
            + "_" + string(alpha) ...
            + ".mat");

        fprintf("\n\n\n\n");
        fprintf("p: %d\n", p);

        result = [];
        result = run_INUDFT2_2D_Condest(xy, n, ...
            min_points, tol_hss, ...
            tol_cg, maxit_cg, ...
            maxit_condest, restarts);

        save("./data/typeII_2d_results_condest" ...
            + "_" + string(p) ...
            + "_" + string(alpha) ...
            + "_" + string(tol_hss) ...
            + ".mat", "result");
    end
end