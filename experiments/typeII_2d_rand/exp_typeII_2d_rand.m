exp_typeII_2d_rand_settings;

for it_tol_hss = 1 : num_tol_hss
    for it_alpha = 1 : num_alpha
        for it_p = num_n : -1 : 1
            tol_hss = tol_hss_list(it_tol_hss);
            alpha = alpha_list(it_alpha);
            p = p_list(it_p);

            load("./data/typeII_2d_points" ...
                + "_" + string(p) ...
                + "_" + string(alpha) ...
                + ".mat");

            fprintf("\n\n\n\n");
            fprintf("p: %d\n", p);

            result = [];
            result = run_INUDFT2_2D(...
                xy, n, ...
                min_points, tol_hss, ...
                tol_cg, maxit_cg, ...
                c_ex, f_ex);

            save("./data/typeII_2d_results" ...
                + "_" + string(p) ...
                + "_" + string(alpha) ...
                + "_" + string(tol_hss) ...
                + ".mat", "result");
        end
    end
end