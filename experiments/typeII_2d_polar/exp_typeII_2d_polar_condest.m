exp_typeII_2d_polar_settings;

for it_beta = 1 : num_beta
    for it_p = 1 : num_n
        tol_hss = tol_hss_list(1);
        beta = beta_list(it_beta);
        p = p_list(it_p);
        n = 2^p;

        load("./data/typeII_2d_points" ...
            + "_" + string(p) ...
            + "_" + string(beta) ...
            + ".mat");

        fprintf("\n\n\n\n");
        fprintf("p: %d\n", p);

        result = [];
        result = run_INUDFT2_2D_Condest(x, n, ...
            min_points, tol_hss, ...
            tol_cg, maxit_cg, ...
            maxit_condest, restarts);

        save("./data/typeII_2d_results_condest" ...
            + "_" + string(p) ...
            + "_" + string(beta) ...
            + "_" + string(tol_hss) ...
            + ".mat", "result");
    end
end